# Codeception Test Suite Configuration
#
# Suite for unit (internal) tests.

class_name: UnitTester
modules:
    enabled:
        - Asserts
        - Helper\Unit
        - Db:
            dsn: 'mysql:host=localhost;dbname=abc;charset=utf8;Type:InnoDB'
            user: 'phalcon'
            password: 'waqas123'
            dump: tests/_data/phalcon01.sql
            populate: true
        - Phalcon:
            bootstrap: app/config/bootstrap.php
            cleanup: true
            savepoints: true